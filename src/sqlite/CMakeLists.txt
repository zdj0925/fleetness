AUX_SOURCE_DIRECTORY(. SRC_LIST)
set(SUB_INCLUDE_DIR
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_CURRENT_BINARY_DIR}
)
message("SUB_INCLUDE_DIR: ${SUB_INCLUDE_DIR}")

add_library(${PROJECT_NAME} DYNAMIC ${SRC_LIST})

target_include_directories(${PROJECT_NAME} PUBLIC ${SUB_INCLUDE_DIR})

